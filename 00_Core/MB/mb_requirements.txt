# Mother-Brain Service — Python dependencies
# Install: pip install -r requirements.txt
#
# Termux note: if uvicorn[standard] fails due to missing C extensions,
# fall back to plain: pip install uvicorn
# (websockets handles WS in that case)

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
aiosqlite>=0.20.0
httpx>=0.27.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.6.0
